#pragma once
// Arquivo onde são definidos os prototipos das funções e constantes significativas da biblioteca pacote
// Essa biblioteca será definida para auxiliar as funções da biblioteca rede

// Protótipos das funções
unsigned int empacotar(int, int, int, int, int);

int passo(unsigned int);

int cor(unsigned int);

int posicao(unsigned int);

int velocidade(unsigned int);

int pista(unsigned int);